#include <fstream>
#include <iostream>
#include <stdio.h>
using namespace std;

int brd_read()
{
    ifstream inpt("input.txt");
    //int i;
    string sg;
    //for (inpt >> sg; !inpt.eof(); inpt >> sg) {
    //    cout << sg << endl;
    //}
    return 0;
}
